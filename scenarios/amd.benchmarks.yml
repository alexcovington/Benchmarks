profiles:
        epyc-lin:
                variables:
                        serverPort: 5000
                        serverAddress: 127.0.0.1
                        cores: 64
                jobs:
                        db:
                                endpoints: 
                                        - http://localhost:5010
                        application:
                                endpoints:
                                        - http://localhost:5010
                                variables:
                                        databaseServer: localhost
                        load:
                                endpoints:
                                        - http://localhost:5010
        local:
                variables:
                        serverPort: 5000
                        serverAddress: 127.0.0.1
                        cores: 64
                jobs:
                        db:
                                endpoints: 
                                        - http://localhost:5010
                        application:
                                endpoints:
                                        - http://localhost:5010
                                variables:
                                        databaseServer: localhost
                        load:
                                endpoints:
                                        - http://localhost:5010
